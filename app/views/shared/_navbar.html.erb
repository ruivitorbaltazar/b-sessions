<div class="navbar-container">
  <div class="navbar-main col-6 mx-auto px-4 d-flex flex-row justify-content-between align-items-center">
    <div class="navbar-brand p-0 m-0">
      <%= link_to meetings_path do %>
        <h3 class="m-0"><i class="fas fa-lightbulb mr-2"></i>b sessions</h3>
      <% end %>
    </div>

    <div class="p-0">
      <% if user_signed_in? %>
        <%= link_to "Edit Profile", edit_user_registration_path, class: 'btn btn-sm btn-outline-secondary' %>
        <%= link_to "Log out", destroy_user_session_path, method: :delete, class: 'btn btn-sm btn-outline-secondary' %>
        <%= link_to 'New Session', new_meeting_path, class: 'btn btn-sm btn-primary' %>
      <% end %>
    </div>
  </div>
</div>
